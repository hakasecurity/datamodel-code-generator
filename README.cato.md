## Deployment instruction
In order to publish this repo to cato's jfrog you need to
1. Run `uv build`
2. Run `uv publish` - Enter you username and auth token that you generated for uploading this package
